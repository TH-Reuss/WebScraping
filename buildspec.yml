version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18.16.0
    commands:
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - npm run start
